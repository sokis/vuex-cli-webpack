// ======================================================
// Overrides when NODE_ENV === 'development'
// ======================================================
export default config => ({
	compiler_public_path: '/',
	compiler_devtool: 'eval'
})
